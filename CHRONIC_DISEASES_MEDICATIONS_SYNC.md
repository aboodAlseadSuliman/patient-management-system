# نظام مزامنة الأمراض المزمنة والأدوية الدائمة
## Chronic Diseases & Permanent Medications Sync System

---

## 🎯 نظرة عامة

تم إضافة نظام ذكي لمزامنة الأمراض المزمنة والأدوية الدائمة بين **ملف المريض** و**سجل الزيارة**، مما يوفر:
- ✅ **مرونة كاملة** - الإضافة من أي مكان (زيارة أو ملف المريض)
- ✅ **تحميل تلقائي** - البيانات تُحمل تلقائياً في الزيارات الجديدة
- ✅ **مزامنة ثنائية الاتجاه** - التعديلات تُحفظ في ملف المريض مباشرة
- ✅ **زر (+) سريع** - إضافة مرض أو دواء جديد للنظام مباشرة

---

## 📋 الميزات المضافة

### 1️⃣ في تاب "الخط الزمني وعوامل الخطورة" - قسم التاريخ المرضي

#### ✨ حقل الأمراض المزمنة
```
🔄 الأمراض المزمنة للمريض
┌───────────────────────────────────────────────┐
│ [Select Multiple]                             │
│ ☑ السكري النوع 2                             │
│ ☑ ارتفاع ضغط الدم                            │
│ ☐ قصور الغدة الدرقية                         │
│                                         [+]   │
└───────────────────────────────────────────────┘
✓ يتم حفظ التعديلات في ملف المريض مباشرة
```

**الميزات:**
- Select Multiple مع بحث
- تحميل تلقائي من ملف المريض
- زر (+) لإضافة مرض جديد للنظام
- المزامنة التلقائية عند الحفظ

#### ✨ حقل الأدوية الدائمة
```
💊 الأدوية الدائمة للمريض
┌───────────────────────────────────────────────┐
│ [Select Multiple]                             │
│ ☑ ميتفورمين (500mg) - Metformin HCL         │
│ ☑ أتورفاستاتين (20mg) - Atorvastatin        │
│ ☐ أملوديبين (5mg) - Amlodipine              │
│                                         [+]   │
└───────────────────────────────────────────────┘
✓ يتم حفظ التعديلات في ملف المريض مباشرة
```

**الميزات:**
- عرض الدواء مع التركيز والاسم العلمي
- تحميل تلقائي من ملف المريض
- زر (+) لإضافة دواء جديد للنظام
- المزامنة التلقائية عند الحفظ

#### 📝 حقول نصية إضافية
```
📝 حالات طبية أخرى (اختياري)
┌───────────────────────────────────────────────┐
│ معلومات طبية إضافية لا تدخل ضمن            │
│ الأمراض المزمنة...                           │
└───────────────────────────────────────────────┘

🏥 الجراحات السابقة
┌───────────────────────────────────────────────┐
│ العمليات الجراحية وتواريخها...               │
└───────────────────────────────────────────────┘
```

---

## 🔧 التنفيذ التقني

### الملفات المعدلة:

#### 1. [TimelineTab.php](app/Filament/Resources/Visits/Schemas/DetailedVisit/TimelineTab.php)
**التعديلات:**
- ✅ إضافة حقل `chronic_diseases_sync` - Select Multiple
- ✅ إضافة حقل `permanent_medications_sync` - Select Multiple
- ✅ تحميل تلقائي من Patient عند اختيار المريض
- ✅ createOptionForm كامل لإضافة مرض/دواء جديد

**الكود الرئيسي:**
```php
Select::make('chronic_diseases_sync')
    ->label('🔄 الأمراض المزمنة للمريض')
    ->multiple()
    ->options(ChronicDisease::query()->where('is_active', true)->pluck('name_ar', 'id'))
    ->searchable()
    ->preload()
    ->default(function (Get $get, $record) {
        // تحميل من ملف المريض
        if ($record && $record->patient_id) {
            return Patient::find($record->patient_id)
                ?->chronicDiseases()
                ->where('patient_chronic_diseases.is_active', true)
                ->pluck('chronic_diseases.id')
                ->toArray() ?? [];
        }
        // ... للإنشاء الجديد
    })
    ->createOptionForm([/* نموذج إضافة مرض جديد */])
    ->createOptionUsing(function (array $data): int {
        $disease = ChronicDisease::create($data);
        Notification::make()->success()->send();
        return $disease->id;
    })
```

#### 2. [CreateVisit.php](app/Filament/Resources/Visits/Pages/CreateVisit.php)
**التعديلات:**
- ✅ إضافة منطق المزامنة في `afterCreate()`
- ✅ استخدام `syncWithoutDetaching` للحفاظ على البيانات الموجودة
- ✅ إضافة تاريخ التشخيص تلقائياً

**الكود الرئيسي:**
```php
protected function afterCreate(): void
{
    $data = $this->form->getState();
    $visit = $this->record;

    // ⭐ مزامنة الأمراض المزمنة
    if (isset($data['chronic_diseases_sync'])) {
        $visit->patient->chronicDiseases()->syncWithoutDetaching(
            collect($data['chronic_diseases_sync'])->mapWithKeys(function ($diseaseId) use ($visit) {
                return [$diseaseId => [
                    'diagnosis_date' => $visit->visit_date,
                    'is_active' => true,
                    'created_at' => now(),
                    'updated_at' => now(),
                ]];
            })->toArray()
        );
    }

    // ⭐ مزامنة الأدوية الدائمة
    if (isset($data['permanent_medications_sync'])) {
        $visit->patient->permanentMedications()->syncWithoutDetaching(
            collect($data['permanent_medications_sync'])->mapWithKeys(function ($medicationId) {
                return [$medicationId => [
                    'is_active' => true,
                    'start_date' => now(),
                    'created_at' => now(),
                    'updated_at' => now(),
                ]];
            })->toArray()
        );
    }
}
```

#### 3. [EditVisit.php](app/Filament/Resources/Visits/Pages/EditVisit.php)
**التعديلات:**
- ✅ إضافة منطق المزامنة في `afterSave()`
- ✅ استخدام `sync` لتحديث القائمة كاملة
- ✅ الحفاظ على البيانات الموجودة (diagnosis_date، dosage، frequency)

**الكود الرئيسي:**
```php
protected function afterSave(): void
{
    $data = $this->form->getState();
    $visit = $this->record;

    // ⭐ مزامنة الأمراض المزمنة
    if (isset($data['chronic_diseases_sync'])) {
        $visit->patient->chronicDiseases()->sync(
            collect($data['chronic_diseases_sync'])->mapWithKeys(function ($diseaseId) use ($visit) {
                $existing = $visit->patient->chronicDiseases()
                    ->where('chronic_disease_id', $diseaseId)
                    ->first();

                return [$diseaseId => [
                    'diagnosis_date' => $existing?->pivot->diagnosis_date ?? $visit->visit_date,
                    'notes' => $existing?->pivot->notes,
                    'is_active' => true,
                    'updated_at' => now(),
                ]];
            })->toArray()
        );
    }
    // ... نفس النمط للأدوية
}
```

#### 4. [TimelineInfoTab.php](app/Filament/Resources/Visits/Schemas/DetailedVisit/InfolistTabs/TimelineInfoTab.php)
**التعديلات:**
- ✅ عرض الأمراض المزمنة من ملف المريض مباشرة
- ✅ عرض الأدوية الدائمة من ملف المريض مباشرة
- ✅ تنسيق احترافي مع badges وأيقونات
- ✅ عرض تاريخ التشخيص والجرعات

**الكود الرئيسي:**
```php
TextEntry::make('patient.chronicDiseases')
    ->label('🔄 الأمراض المزمنة')
    ->badge()
    ->separator(',')
    ->formatStateUsing(function ($record) {
        if (!$record || !$record->patient) {
            return 'لا توجد أمراض مزمنة';
        }

        $diseases = $record->patient->chronicDiseases()
            ->where('patient_chronic_diseases.is_active', true)
            ->get();

        return $diseases->map(function ($disease) {
            $text = $disease->name_ar;
            if ($disease->pivot->diagnosis_date) {
                $text .= ' (منذ ' . $disease->pivot->diagnosis_date . ')';
            }
            return $text;
        })->implode(' • ');
    })
    ->color('warning')
    ->icon('heroicon-o-exclamation-triangle')
```

---

## 🔄 تدفق العمل (Workflow)

### سيناريو 1: مريض جديد - أول زيارة

```
1. الطبيب يختار "إضافة زيارة جديدة"

2. يختار المريض من القائمة

3. ينتقل لتاب "الخط الزمني" → قسم "التاريخ المرضي"

4. يضغط على حقل "الأمراض المزمنة"
   ✓ القائمة فارغة (مريض جديد)

5. يضغط على زر (+) ويضيف:
   - السكري النوع 2
   - ارتفاع ضغط الدم

6. يختار الأمراض المضافة

7. يضغط على حقل "الأدوية الدائمة"
   ✓ القائمة فارغة (مريض جديد)

8. يضغط على زر (+) ويضيف:
   - ميتفورمين 500mg
   - أملوديبين 5mg

9. يختار الأدوية المضافة

10. يكمل باقي الزيارة ويحفظ

✅ النتيجة:
   - تم حفظ الزيارة ✓
   - تم إضافة الأمراض لملف المريض ✓
   - تم إضافة الأدوية لملف المريض ✓
   - تاريخ التشخيص = تاريخ هذه الزيارة ✓
```

### سيناريو 2: مريض موجود - زيارة متابعة

```
1. الطبيب يختار "إضافة زيارة جديدة"

2. يختار مريض موجود (عنده أمراض وأدوية مسجلة)

3. ينتقل لتاب "الخط الزمني" → قسم "التاريخ المرضي"

✅ يجد الحقول مملوءة تلقائياً:
   - الأمراض المزمنة: ☑ السكري ☑ ضغط الدم
   - الأدوية الدائمة: ☑ ميتفورمين ☑ أملوديبين

4. يقرر إضافة مرض جديد:
   - يضغط زر (+)
   - يضيف "قصور الغدة الدرقية"
   - يختاره من القائمة

5. يحفظ الزيارة

✅ النتيجة:
   - تم تحديث ملف المريض ✓
   - المريض الآن عنده 3 أمراض مزمنة ✓
   - الزيارات القادمة ستعرض الثلاثة تلقائياً ✓
```

### سيناريو 3: تعديل زيارة موجودة

```
1. الطبيب يفتح زيارة قديمة للتعديل

2. ينتقل لتاب "الخط الزمني"

✅ يجد الحقول مملوءة بالأمراض والأدوية من ملف المريض

3. يقرر حذف دواء (المريض أوقفه):
   - يزيل العلامة عن "أملوديبين"

4. يضيف دواء جديد:
   - يضيف "إنسولين"

5. يحفظ

✅ النتيجة:
   - تم تحديث ملف المريض ✓
   - أملوديبين أصبح غير نشط ✓
   - إنسولين مضاف كدواء دائم ✓
   - التعديلات تظهر في جميع الزيارات القادمة ✓
```

---

## 🎨 واجهة المستخدم

### في نموذج الإدخال (Form):

```
┌─────────────────────────────────────────────────────────┐
│ 📋 التاريخ المرضي                                      │
│ السوابق الطبية والجراحية - يتم المزامنة تلقائياً      │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ 🔄 الأمراض المزمنة للمريض                             │
│ ┌───────────────────────────────────────────────────┐  │
│ │ 🔍 ابحث أو اختر...                          [+] │  │
│ │ ☑ السكري النوع 2                                 │  │
│ │ ☑ ارتفاع ضغط الدم                                │  │
│ │ ☐ قصور الغدة الدرقية                             │  │
│ │ ☐ القولون العصبي                                 │  │
│ └───────────────────────────────────────────────────┘  │
│ ✓ يتم حفظ التعديلات في ملف المريض | زر (+) للإضافة    │
│                                                         │
│ 💊 الأدوية الدائمة للمريض                             │
│ ┌───────────────────────────────────────────────────┐  │
│ │ 🔍 ابحث أو اختر...                          [+] │  │
│ │ ☑ ميتفورمين (500mg) - Metformin HCL            │  │
│ │ ☑ أملوديبين (5mg) - Amlodipine                 │  │
│ │ ☐ أتورفاستاتين (20mg) - Atorvastatin           │  │
│ └───────────────────────────────────────────────────┘  │
│ ✓ يتم حفظ التعديلات في ملف المريض | زر (+) للإضافة    │
│                                                         │
│ 📝 حالات طبية أخرى (اختياري)                          │
│ ┌───────────────────────────────────────────────────┐  │
│ │ معلومات إضافية...                                 │  │
│ └───────────────────────────────────────────────────┘  │
│                                                         │
│ 🏥 الجراحات السابقة                                   │
│ ┌───────────────────────────────────────────────────┐  │
│ │ العمليات الجراحية...                              │  │
│ └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### في صفحة العرض (View):

```
┌─────────────────────────────────────────────────────────┐
│ 📋 التاريخ المرضي                                      │
│ السوابق الطبية والجراحية من ملف المريض                 │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ 🔄 الأمراض المزمنة                                     │
│ ┌───────────────────────────────────────────────────┐  │
│ │ السكري النوع 2 (منذ 2020-01-15) •                │  │
│ │ ارتفاع ضغط الدم (منذ 2019-06-20)                  │  │
│ └───────────────────────────────────────────────────┘  │
│                                                         │
│ 💊 الأدوية الدائمة                                     │
│ ┌───────────────────────────────────────────────────┐  │
│ │ ميتفورمين (500mg) - 500mg, مرتين يومياً •        │  │
│ │ أملوديبين (5mg) - 5mg, مرة يومياً                │  │
│ └───────────────────────────────────────────────────┘  │
│                                                         │
│ 🏥 الجراحات السابقة                                    │
│ استئصال المرارة - 2015                                 │
└─────────────────────────────────────────────────────────┘
```

---

## ✅ المزايا الرئيسية

### 1. **مرونة كاملة**
- ✅ الإضافة من أي مكان (زيارة أو ملف المريض)
- ✅ التعديل في أي وقت
- ✅ الحذف/التعطيل سهل

### 2. **توفير الوقت**
- ✅ تحميل تلقائي في الزيارات الجديدة
- ✅ لا حاجة لإعادة الكتابة
- ✅ إضافة سريعة عبر زر (+)

### 3. **دقة البيانات**
- ✅ لا أخطاء إملائية
- ✅ بيانات موحدة
- ✅ تواريخ تشخيص دقيقة

### 4. **إحصائيات ممكنة**
- ✅ أكثر الأمراض شيوعاً
- ✅ أكثر الأدوية استخداماً
- ✅ تتبع المرضى حسب المرض

---

## 🔍 نقاط مهمة

### ⚠️ الفرق بين Sync و SyncWithoutDetaching

#### في `CreateVisit` استخدمنا `syncWithoutDetaching`:
```php
$visit->patient->chronicDiseases()->syncWithoutDetaching([...]);
```
**السبب**: لا نريد حذف الأمراض القديمة - فقط إضافة جديدة

#### في `EditVisit` استخدمنا `sync`:
```php
$visit->patient->chronicDiseases()->sync([...]);
```
**السبب**: نريد تحديث القائمة كاملة - ما تم اختياره فقط يبقى نشط

### 🎯 الحفاظ على البيانات الموجودة

في `EditVisit` نتحقق من البيانات الموجودة قبل المزامنة:
```php
$existing = $visit->patient->chronicDiseases()
    ->where('chronic_disease_id', $diseaseId)
    ->first();

return [$diseaseId => [
    'diagnosis_date' => $existing?->pivot->diagnosis_date ?? $visit->visit_date,
    'notes' => $existing?->pivot->notes,
    // ...
]];
```

هذا يضمن عدم فقدان:
- تاريخ التشخيص الأصلي
- الملاحظات المضافة
- أي بيانات إضافية

---

## 🚀 الخطوات القادمة المقترحة

### المرحلة التالية - تحسينات إضافية:

1. **صفحة الأمراض المزمنة** - إدارة مركزية للأمراض
2. **صفحة الأدوية** - إدارة مركزية للأدوية
3. **التقارير** - تقرير بالمرضى حسب المرض
4. **الإحصائيات** - أكثر الأمراض/الأدوية شيوعاً
5. **التنبيهات** - تحذير عند تعارض الأدوية

---

## 📝 ملاحظات للمطورين

### العلاقات المستخدمة:

```php
// في Patient Model
public function chronicDiseases()
{
    return $this->belongsToMany(ChronicDisease::class, 'patient_chronic_diseases')
        ->withPivot(['diagnosis_date', 'notes', 'is_active'])
        ->withTimestamps();
}

public function permanentMedications()
{
    return $this->belongsToMany(Medication::class, 'patient_permanent_medications')
        ->withPivot(['dosage', 'frequency', 'route', 'start_date', 'notes', 'is_active'])
        ->withTimestamps();
}
```

### الجداول المستخدمة:

1. `chronic_diseases` - جدول الأمراض المزمنة الرئيسي
2. `medications` - جدول الأدوية الرئيسي
3. `patient_chronic_diseases` - جدول الربط (pivot)
4. `patient_permanent_medications` - جدول الربط (pivot)

---

**تم التطوير بنجاح! 🎉**

🤖 Generated with [Claude Code](https://claude.com/claude-code)
